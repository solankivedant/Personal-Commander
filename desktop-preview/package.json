{
  "name": "munshiji-desktop-preview",
  "private": true,
  "version": "0.1.0",
  "description": "Tauri shell wrapping the Munshiji Control Center UI preview — no real backend, see README.md",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon src-tauri/icon-source.png -o src-tauri/icons",
    "web": "node serve.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
